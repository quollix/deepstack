packages:
  github.com/quollix/deepstack:
    interfaces:
      LoggingBackend:
        config:
          filename: backend_mock.go
          structname: LoggingBackendMock
      StackTracer:
        config:
          filename: stack_tracer_mock.go
          structname: StackTracerMock
